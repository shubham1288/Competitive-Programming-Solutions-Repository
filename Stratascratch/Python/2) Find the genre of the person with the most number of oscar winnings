Question - https://platform.stratascratch.com/coding/10171-find-the-genre-of-the-person-with-the-most-number-of-oscar-winnings?code_type=3

# Import your libraries
import pandas as pd

# Start writing code
oscar_nominees.head()

# Count Oscar wins per nominee
wins = oscar_nominees[oscar_nominees['winner'] == 1] \
        .groupby('nominee').size().reset_index(name='wins')

# Sort by highest wins and alphabetical nominee name
top_person = wins.sort_values(['wins', 'nominee'], ascending=[False, True]).head(1)

# Join with nominee_information to get genre
result = top_person.merge(nominee_information, left_on='nominee', right_on='name')

# Return expected output
result[['top_genre']]
